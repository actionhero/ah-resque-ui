import React from 'react'
import ReactDOM from 'react-dom'
import App from './router'
// import 'babel-polyfill' (moved to webpack config)

ReactDOM.render(<App />, document.getElementById('application'))
